title: computer_picture
description: this is a reposity for computer wall
template:
  repo: computer_picture-template
  name: computer_picture
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
